import { TypingEffectDirective } from './typing-effect.directive';

describe('TypingEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new TypingEffectDirective();
    expect(directive).toBeTruthy();
  });
});
